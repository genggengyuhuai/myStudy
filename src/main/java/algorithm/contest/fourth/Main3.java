package algorithm.contest.fourth;

/**
 * 按照要求保留数组元素使得和最大
 * Given an array of N numbers, we need to maximize the sum of selected numbers. At each step, you need to select a number Ai,
 * delete one occurrence of Ai-1 and Ai+1 (if exists) and Ai each from the array. Repeat these steps until the array gets empty. The problem is to maximize the sum of selected numbers.
 * @author lihaoyu
 * @date 2019/11/21 10:39
 */
public class Main3 {

    public static void main(String[] args) {

//        case = int(input())
//        while case:
//        length = int(input())
//        array = [int(x) for x in input().split()]
//        ans = dict.fromkeys(range(0, 101), 0)
//        for i in range(length):
//        ans[array[i]] += 1
//        maximum = max(array)
//        for i in range(2, maximum + 1):
//        ans[i] = max(ans[i - 1], ans[i - 2] + ans[i] * i)
//        print(ans[maximum])
//        case -= 1

    }
}
